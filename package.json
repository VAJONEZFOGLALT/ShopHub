{
  "name": "project",
  "version": "1.0.0",
  "description": "E-commerce platform with React frontend and NestJS backend",
  "scripts": {
    "postinstall": "node setup.js",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "predev:backend": "node scripts/kill-port.js 3000",
    "local:setup": "npm run db:generate && npm run db:migrate",
    "local:dev": "npm run local:setup && npm run dev",
    "dev:backend": "cd backend && npm run start:dev",
    "dev:frontend": "cd frontend && npm run dev",
    "build": "npm run build:backend && npm run build:frontend",
    "build:backend": "cd backend && npm run build",
    "build:frontend": "cd frontend && npm run build",
    "db:migrate": "cd backend && npx prisma migrate deploy",
    "db:generate": "cd backend && npx prisma generate",
    "db:seed": "cd backend && npm run seed"
  },
  "keywords": [
    "e-commerce",
    "react",
    "nestjs",
    "prisma"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
